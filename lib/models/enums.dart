enum Materia {
  matematica,
  portugues,
  historia,
  geografia,
  biologia,
  fisica,
  quimica,
  ingles,
}

enum StatusSimulado {
  disponivel,
  emAndamento,
  concluido,
}

enum Alternativa { A, B, C, D, E }